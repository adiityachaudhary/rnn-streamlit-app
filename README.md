# 🧠 RNN Interaction Predictor

This app predicts whether a user's next interaction will be a **view** or **cart/purchase**, based on a session of past interactions.

**Usage**: Use the interactive dropdowns to build a session and click “Predict”!

Built with TensorFlow RNN + Streamlit.
